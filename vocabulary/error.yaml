types:
  - Object

description: >
  A container for an error. It **MUST** exist only at the top-level document.
  The contents of the error object are opaque to this specification. For
  example:


  ```json

  {
    "error": {
      "label": "NotFoundError",
      "comment": "A requested resource was not found."
    }
  }

  ```
